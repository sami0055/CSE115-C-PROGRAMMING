main()
{
    int i,j,a,b,count=0;
    printf("Enter the value of a and b: ");
    scanf("%d%d",&a,&b);


    for(i=1; i<=a,i<=b; i++)

    {
        if(a%i==0 && b%i==0 && i==i)
        {
            count++;
        }


    }
    printf(" Number of common factor is %d",count);


}
